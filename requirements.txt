# Python dependencies for the ML notebooks under ml/notebook/.
# The merge_person_datasets_folders.py helper uses only the Python standard library.
torch>=2.0
torchvision>=0.15
ultralytics>=8.3
matplotlib>=3.7
ipykernel>=6.0
