{
  "name": "wardy",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8000 --directory apps",
    "test": "node --test tests/unit/*.test.mjs tests/integration/*.test.mjs",
    "test:edge-camera": "cmake -S edge -B edge/build && cmake --build edge/build && ctest --test-dir edge/build --output-on-failure"
  }
}
